<!-- < %= form_tag "/cats", method: :post  do %>
  < %= label_tag :name %>
  < %= text_field_tag :name %>
  < %= submit_tag "Create Cat" %>
< % end %> -->

<%= form_for @cats do |f|  %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>
